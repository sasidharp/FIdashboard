export interface Job {
    subprocess: string;
    running: string;
    ready: string;
    scheduled: string;
    released: string;
    aborted: string;
    finished: string;
    putactive: string;
}
